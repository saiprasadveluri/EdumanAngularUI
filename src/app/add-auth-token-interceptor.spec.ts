import { AddAuthTokenInterceptor } from './add-auth-token-interceptor';

describe('AddAuthTokenInterceptor', () => {
  it('should create an instance', () => {
    expect(new AddAuthTokenInterceptor()).toBeTruthy();
  });
});
